# Build Commands (run from /build folder)
```bash
xcodebuild -scheme ComputerGraphics -configuration Debug build    # Build Debug
xcodebuild -scheme ComputerGraphics -configuration Release build  # Build Release
./Debug/ComputerGraphics                                          # Run Debug
./Release/ComputerGraphics                                        # Run Release
```
